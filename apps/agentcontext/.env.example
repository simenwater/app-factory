# GitHub Token (可选，用于私有仓库和更高 API 限制)
GITHUB_TOKEN=

# OpenAI API Key (可选，用于 LLM 增强生成；不配置则使用模板生成)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Stripe (可选，用于付费功能)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# App URL
NEXT_PUBLIC_APP_URL=http://localhost:3000
